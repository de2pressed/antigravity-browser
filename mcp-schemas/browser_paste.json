{
  "name": "browser_paste",
  "description": "Paste text or TSV (Tab-Separated Values) directly into the active element/selection in the page via clipboard and hardware Ctrl+V.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "tabId": {
        "type": "number",
        "description": "The target tab ID."
      },
      "text": {
        "type": "string",
        "description": "The text or TSV table content to paste."
      }
    },
    "required": ["tabId", "text"]
  }
}
